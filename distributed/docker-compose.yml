version: '3'
services:
  flask:
    image: "comporg/flask:${TAG}"
    ports:
      - 5050:5050
    depends_on:
      - redis
      # Add celery workers here:
      # - {project_name}_tasks
      - compbaseball_tasks
  # configure celery workers:
  # {project_name}_tasks:
  #   image: "comporg/{project_name}_tasks:${TAG}"
  #   depends_on:
  #     redis
  compbaseball_tasks:
    image: "comporg/compbaseball_tasks:${TAG}"
    depends_on:
      - redis
  redis:
    image: redis
