dist: xenial
language: python
python:
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - ./python_env.sh

# command to run tests
script:
  - pytest webapp/apps/ -v -m "not requires_stripe"